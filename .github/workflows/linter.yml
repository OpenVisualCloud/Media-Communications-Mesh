name: Linter
on:
  pull_request:
  workflow_call:
  workflow_dispatch:
    inputs:
      branch:
        description: "Branch to run the scans on"
        default: "main"
        type: string

permissions:
  contents: read
jobs:
  super-linter:
    name: "super-linter: Workflow initializing"
    runs-on: "ubuntu-22.04"
    timeout-minutes: 30
    permissions:
      contents: read
    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@17d0e2bd7d51742c71671bd19fa12bdc9d40a3d6 # v2.8.1
        with:
          egress-policy: audit

      - name: checkout repository
        uses: actions/checkout@692973e3d937129bcbf40652eb9f2f61becf3332 # v4.1.7
        with:
          fetch-depth: 0
          ref: "${{ env.INPUT_BRANCH }}"

      - name: Lint
        uses: super-linter/super-linter/slim@85f7611e0f7b53c8573cca84aa0ed4344f6f6a4d # v7.2.1 x-release-please-version
        env:
          DISABLE_ERRORS: false
          VALIDATE_ALL_CODEBASE: false
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          BASH_SEVERITY: "warning"
          LINTER_RULES_PATH: ".github/configs"
          # Disable GitHub status updates to avoid permission issues
          GITHUB_ACTIONS_COMMAND_ARGS: "--disable-lock"
          GITHUB_COMMENT_ON_PR: false
          GITHUB_STATUS_UPDATES: false
          VALIDATE_BASH_EXEC: true
          VALIDATE_BASH: true
          VALIDATE_CLANG_FORMAT: true
          VALIDATE_CPP: true
          VALIDATE_GO: true
          VALIDATE_GITHUB_ACTIONS: true
          VALIDATE_GROOVY: true
          VALIDATE_JSON_PRETTIER: true
          VALIDATE_JSONC_PRETTIER: true
          VALIDATE_MARKDOWN: true
          VALIDATE_PROTOBUF: true
          VALIDATE_PYTHON_BLACK: true
          VALIDATE_YAML_PRETTIER: true

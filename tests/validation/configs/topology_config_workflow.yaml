---
metadata:
  version: '2.4'
hosts:
  - name: mesh-agent
    instantiate: true
    role: sut
    network_interfaces:
      - pci_device: 8086:1592
        interface_index: 0 # all
    connections:
      - ip_address: 127.0.0.1
        connection_type: LocalConnection
    extra_info:
      mesh_agent:
        control_port: 8100
        proxy_port: 50051
      media_proxy:
        sdk_port: 8002
